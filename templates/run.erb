#!/bin/sh
exec 2>&1
set -e

cd "<%= @rundir %>"
echo "Starting <%= @name %> in `/bin/pwd` ..."

<% if @source_file -%>
. <%= @source_file %>
<% end -%>

set -x
exec chpst -e env -u <%= @user %>:<%= @group %> <%= @command %>
