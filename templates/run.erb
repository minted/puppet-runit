#!/bin/sh
exec 2>&1
set -e

envdir="`/bin/pwd`/env"
cd "<%= @rundir %>"
echo "*** Starting <%= @name %> in `/bin/pwd` ..."

<% if @inject -%>
. <%= @inject %>
<% end -%>

set -x
exec chpst -e "$envdir" -u <%= @user %>:<%= @group %> -- <%= @command %>
